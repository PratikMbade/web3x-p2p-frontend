import {
  base64ToString
} from "./chunk-43F4DZXD.js";

// node_modules/thirdweb/dist/esm/utils/base64/base64.js
var Base64Prefix = "data:application/json;base64";
function isBase64JSON(input) {
  if (input.toLowerCase().startsWith(Base64Prefix)) {
    return true;
  }
  return false;
}
function parseBase64String(input) {
  const commaIndex = input.indexOf(",");
  const base64 = input.slice(commaIndex + 1);
  return base64ToString(base64);
}

// node_modules/thirdweb/dist/esm/utils/utf8/utf8.js
var UTF8Prefix = "data:application/json;utf-8";
function isUTF8JSONString(input) {
  if (input.toLowerCase().startsWith(UTF8Prefix)) {
    return true;
  }
  return false;
}
function parseUTF8String(input) {
  const commaIndex = input.indexOf(",");
  const utf8 = input.slice(commaIndex + 1);
  try {
    return decodeURIComponent(utf8);
  } catch {
    return utf8;
  }
}

export {
  isBase64JSON,
  parseBase64String,
  isUTF8JSONString,
  parseUTF8String
};
//# sourceMappingURL=chunk-WU2ORJSW.js.map
